import GuestSelector from "./GuestSelector";

function SearchBar() {
  return (
    <div>
      <button>Where</button>
      <button>When</button>
      <GuestSelector />
    </div>
  );
}

export default SearchBar;
